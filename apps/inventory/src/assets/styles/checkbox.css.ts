import { css } from "../../../styled-system/css";

export const checkboxContainer = css({
    display: "flex",
    flexDirection: "row",
    columnGap: "15px",
    alignItems: "center",
})

export const checkboxIcon = css({
    md: {
        cursor: "pointer"
    }
})